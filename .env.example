# ── Anthropic ────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=

# ── Kroger (live API — remains valid) ────────────────────────────────────────
KROGER_CLIENT_ID=
KROGER_CLIENT_SECRET=
KROGER_LOCATION_ID=02400688   # Fred Meyer Seattle default

# ── Garmin (optional automated path via garth) ────────────────────────────────
GARMIN_EMAIL=
GARMIN_PASSWORD=

# ── Strava (official API — optional automated path) ───────────────────────────
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REFRESH_TOKEN=

# ── Gmail (real send via SMTP App Password) ───────────────────────────────────
# Setup: myaccount.google.com → Security → App Passwords → Create (requires 2FA)
GMAIL_SENDER=
GMAIL_APP_PASSWORD=          # 16-char App Password (spaces ok)
GMAIL_RECIPIENT=             # defaults to GMAIL_SENDER if blank

# ── Defaults ─────────────────────────────────────────────────────────────────
DEFAULT_STORE=fred_meyer
DEFAULT_SERVINGS=2
